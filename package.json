{
  "name": "learnguitar",
  "version": "1.0.0",
  "description": "An app for Learning Guitar",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "nodemon server/server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },

  "dependencies": {
    "nodemon": "^3.0.1"
  },

  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
